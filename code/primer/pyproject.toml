[project]
name = "primer-irctc"
version = "0.1.0"
description = "PRIMER loop that calls the dummy IRCTC API (train status, PNR, train mapping)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "openai>=1.0.0",
]

[project.scripts]
primer = "app:main"
